The primary purpose of this IP Logger app is to track the IPv4 address assigned to the device by a Wi-Fi network. In networks that use sign-in based authentication systems, the DHCP server may assign a new IP address to the device over time. By monitoring these changes, the app allows users to log out their credentials from the previously assigned IPv4 address whenever a new IP is allocated, helping maintain proper session management and avoid authentication conflicts.